// Sitewide entry — every page loads this one module.
// Header/footer markup is static per page (kept in sync) so navigation
// works without JavaScript; these modules only add behavior.

import './nav.js';
import './reveal.js';
import './marquee.js';
import './cursor.js';
import './rail.js';
import './countdown.js';
